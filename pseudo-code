?? while ( playerHealth > 0 or npcHealth > 0 ) ???

check if user presses "ROCK", "PAPER", "SCISSORS"
    play corresponding animation
    
    set playerChoice to "ROCK = 1" "PAPER = 2" "SCISSORS = 0"
    get randi_range(0, 2) for npcChoice
    
    compare playerChoice to npcChoice
        if player beats npc
            display a win message
            npcHealth = npcHealth - 1
        elif player loses to npc
            display a lose message
            playerHealth = playerHealth - 1
        elif player and npc are the same
            display a draw message
    
if playerHealth or npcHealth = 0
    explode the corresponding hand
    reset the game
    
  
